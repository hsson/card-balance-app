<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.creotec.chscardbalance2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GlobalState"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".controller.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".controller.RestaurantPageActivity"
            android:theme="@style/AppTheme.Transparent.StatusBar" />
        <activity android:name=".controller.AppUpgradedFromLegacyActivity"
            android:theme="@style/AppTheme" />

        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.BalanceService"
            android:exported="false" />
        <service
            android:name=".service.MenuService"
            android:exported="false" />
    </application>

</manifest>